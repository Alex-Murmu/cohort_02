command to learn 

1.  pwd => print the current working directoy address
2.  ls  => will give you the list of directories or files present on that directories
3. ls -l  => is used to list file and directory in long format it provdes detailed information about each 
   file and directories in columnar format
4. ls -lh  ==> same as above one bit human readable
5.  ls -R <folder>  => list all the subdirectories and file 
6. ls -lt=> how we can get a list of dorectories and see the last modification of that file or folder
7. ls -la ==> to see hidden file
8.  